import 'package:flutter/material.dart';

class ShowsPage extends StatelessWidget {
  const ShowsPage({Key? key}) : super(key: key);

  @override
  Widget build(context) {
    return Placeholder(child: Text('сериалы'));
  }
}
