import 'package:flutter/material.dart';

class OckgHomePage extends StatelessWidget {
  const OckgHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}